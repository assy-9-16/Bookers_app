<h1>本のタイトル</h1>
<p><%= @book.title %></p>

<h2>感想</h2>
<P><%= @book.body %></P>
<%= link_to "編集", edit_book_path(@book.id) %>
<%= link_to "削除", book_path(@book.id), method: :delete, "data-confirm" => "本当に削除しますか？" %>